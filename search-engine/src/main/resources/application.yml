spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/search_engine

  jpa:
    hibernate:
      ddl-auto: validate
      use-new-id-generator-mappings: false

page-rank:
  enabled: false